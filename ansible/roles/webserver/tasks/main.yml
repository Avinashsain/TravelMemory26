---
# roles/webserver/tasks/main.yml
# Ubuntu 24 + apt

# ── 1. System update & packages ───────────────────────────────────────────
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true

- name: Install base packages
  ansible.builtin.apt:
    name:
      - git
      - curl
      - nginx
      - nodejs
      - npm
    state: present

# ── 2. Clone the repository ────────────────────────────────────────────────
- name: Clone TravelMemory repository
  ansible.builtin.git:
    repo: "https://github.com/Avinashsain/TravelMemory.git"
    dest: /root/TravelMemory
    version: main
    force: true

# ── 3. Backend setup ───────────────────────────────────────────────────────
- name: Deploy backend .env file
  ansible.builtin.copy:
    dest: /root/TravelMemory/backend/.env
    content: |
      PORT=3001
      MONGO_URI={{ mongo_uri }}
    mode: "0640"

- name: Install backend dependencies
  ansible.builtin.command:
    cmd: npm install
    chdir: /root/TravelMemory/backend
  changed_when: true

# ── 4. Frontend setup ──────────────────────────────────────────────────────
- name: Deploy frontend .env file
  ansible.builtin.copy:
    dest: /root/TravelMemory/frontend/.env
    content: |
      REACT_APP_BACKEND_URL=http://{{ ansible_host }}:3001
    mode: "0640"

- name: Install frontend dependencies
  ansible.builtin.command:
    cmd: npm install
    chdir: /root/TravelMemory/frontend
  changed_when: true

- name: Build React frontend
  ansible.builtin.command:
    cmd: npm run build
    chdir: /root/TravelMemory/frontend
  environment:
    CI: "false"
  changed_when: true

# ── 5. Install PM2 and serve globally ─────────────────────────────────────
- name: Install PM2 globally
  ansible.builtin.command: npm install -g pm2
  changed_when: true

- name: Install serve globally
  ansible.builtin.command: npm install -g serve
  changed_when: true

# ── 6. Start apps with PM2 ────────────────────────────────────────────────
- name: Stop existing PM2 processes
  ansible.builtin.command: pm2 delete all
  changed_when: true
  failed_when: false

- name: Start backend with PM2
  ansible.builtin.command:
    cmd: pm2 start index.js --name "travel-backend"
    chdir: /root/TravelMemory/backend
  changed_when: true

- name: Start frontend with PM2
  ansible.builtin.command:
    cmd: pm2 start serve --name "travel-frontend" -- -s build -l 3000
    chdir: /root/TravelMemory/frontend
  changed_when: true

- name: Save PM2 process list
  ansible.builtin.command: pm2 save
  changed_when: true

- name: Generate PM2 startup script
  ansible.builtin.command: pm2 startup systemd -u root --hp /root
  changed_when: true
  register: webserver_pm2_startup_output

- name: Enable PM2 systemd service
  ansible.builtin.systemd:
    name: pm2-root
    enabled: true
    state: started
  changed_when: true
  failed_when: false

# ── 7. NGINX reverse proxy ─────────────────────────────────────────────────
- name: Deploy NGINX config
  ansible.builtin.copy:
    dest: /etc/nginx/sites-available/default
    content: |
      server {
          listen 80;
          server_name _;

          location / {
              proxy_pass http://127.0.0.1:3000;
              proxy_http_version 1.1;
              proxy_set_header Host $host;
              proxy_set_header X-Real-IP $remote_addr;
              proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
              proxy_set_header X-Forwarded-Proto $scheme;
          }

          location /api/ {
              proxy_pass http://127.0.0.1:3001;
              proxy_http_version 1.1;
              proxy_set_header Host $host;
              proxy_set_header X-Real-IP $remote_addr;
              proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
              proxy_set_header X-Forwarded-Proto $scheme;
          }
      }
    mode: "0644"

- name: Test NGINX config
  ansible.builtin.command: nginx -t
  changed_when: false

- name: Restart NGINX
  ansible.builtin.systemd:
    name: nginx
    enabled: true
    state: restarted

# ── 8. Security hardening ──────────────────────────────────────────────────
- name: Disable root SSH login
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: "^PermitRootLogin"
    line: "PermitRootLogin no"

- name: Disable password authentication
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: "^PasswordAuthentication"
    line: "PasswordAuthentication no"

- name: Restart SSH
  ansible.builtin.systemd:
    name: ssh
    state: restarted
